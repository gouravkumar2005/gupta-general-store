; This machine has NODE_ENV=production set globally, which makes npm default
; to omitting devDependencies (tsx, typescript, prisma CLI, @types/*, eslint,
; etc.) on every install. Force them to always be included for this project.
include=dev
